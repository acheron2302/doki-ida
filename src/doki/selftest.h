//----------------------------------------------------------------------------
// Doki Theme - runtime self-tests (color conversion + palette completeness).
// Invoked via run(arg == 1). Returns true if all checks pass.
//----------------------------------------------------------------------------
#pragma once

namespace doki
{
bool run_selftest();
}
